name: Makefile CI

on:
  push:
    branches: [ "slaveholder" ]
  pull_request:
    branches: [ "slaveholder" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: compile check
      run: make all
